/** Тренажёр: тема «JavaScript: замыкания». */

export const jsClosuresTopic = "JavaScript: замыкания";

export const jsClosuresTrainerQuestions = [
  {
    topic: jsClosuresTopic,
    question: "Своими словами: что такое замыкание (closure) в JavaScript?",
    hint: "Свяжите функцию с окружением, где она объявлена, и с доступом к переменным после выхода из внешней функции.",
    answer: `Замыкание — когда функция сохраняет доступ к переменным из внешних (охватывающих) областей видимости, даже после того, как код внешней функции уже отработал. Это следует из того, что у функции есть ссылка на своё лексическое окружение; пока жива сама функция (или возвращённая изнутри внутренняя), живёт и нужное окружение для разрешения имён вроде свободных переменных.`,
  },
  {
    topic: jsClosuresTopic,
    question:
      "Почему часто `for (var i = 0; i < 3; i++) { setTimeout(() => console.log(i), 0); }` печатает три одинаковых числа, а с `let` — 0, 1, 2?",
    hint: "Область видимости var vs блочная область let на каждой итерации.",
    answer: `У **var** область видимости — вся функция-обёртка (или глобальная), поэтому в цикле одна общая переменная **i**. К моменту выполнения колбэков \`setTimeout\` цикл уже закончился, и **i** равно 3 — три колбэка читают одно и то же значение.

У **let**/**const** на каждой итерации цикла своё блочное окружение со своей **i**, и каждый колбэк замыкается на свою копию — получаем 0, 1, 2.`,
  },
  {
    topic: jsClosuresTopic,
    question:
      "Как без смены `var` на `let` исправить вывод в цикле с `setTimeout`, используя замыкание?",
    hint: "Нужно создать новое окружение на каждой итерации — IIFE или отдельная функция с параметром.",
    answer: `Примеры:

**IIFE с параметром:**
\`\`\`javascript
for (var i = 0; i < 3; i++) {
  (function (j) {
    setTimeout(() => console.log(j), 0);
  })(i);
}
\`\`\`

**Отдельная функция:**
\`\`\`javascript
function schedule(j) {
  setTimeout(() => console.log(j), 0);
}
for (var i = 0; i < 3; i++) schedule(i);
\`\`\`

На каждом вызове создаётся своё локальное **j**, на которое замыкается колбэк.`,
  },
  {
    topic: jsClosuresTopic,
    question: "Что такое IIFE и как она связана с замыканиями и изоляцией переменных?",
    hint: "Функция вызывается сразу; внутри — своё лексическое окружение.",
    answer: `**IIFE** (Immediately Invoked Function Expression) — объявление функции и немедленный вызов: \`(function() { ... })();\`. Внутри создаётся новое лексическое окружение: переменные не попадают в глобальную область (до модулей ES это был способ избежать загрязнения \`window\`). Внешний код не видит внутренние \`const\`/\`let\`, но возвращённые из IIFE функции могут продолжать иметь к ним доступ — классическое замыкание.`,
  },
  {
    topic: jsClosuresTopic,
    question:
      "Что произойдёт с переменными внешней функции, если вернуть из неё внутреннюю функцию и вызвать её позже?",
    hint: "Внешняя функция уже завершилась, но окружение может сохраняться.",
    answer: `Внутренняя функция хранит ссылку на лексическое окружение внешней. Пока жива внутренняя функция, не собирается сборщиком мусора то окружение (или его часть), необходимое для разрешения переменных при вызове. Поэтому счётчики и «приватные» поля в фабриках работают: каждый вызов внешней функции может создать новое окружение и новую внутреннюю функцию со своим состоянием.`,
  },
  {
    topic: jsClosuresTopic,
    question:
      "Как замыкания связаны с функциями высшего порядка (передача функции как аргумента или возврат функции)?",
    hint: "При возврате функции из функции возвращаемое значение тащит окружение с собой.",
    answer: `Если функция **возвращает** другую функцию или **принимает** колбэк, замыкания неизбежны: возвращённая функция помнит переменные на момент создания; колбэк, переданный в \`map\`/\`setTimeout\`, захватывает переменные из места объявления. Это основа декораторов, частичного применения, колбэков в асинхронном коде и хуков в React (внутри — своё состояние и очередь обновлений в окружении библиотеки).`,
  },
  {
    topic: jsClosuresTopic,
    question:
      "Какие типичные последствия замыканий для потребления памяти? Когда стоит опасаться «утечки»?",
    hint: "Пока держится функция с замыканием, может удерживаться большой объект из внешней области.",
    answer: `Если внутренняя функция долго живёт (глобальный колбэк, подписка, обработчик) и замыкается на большой объект из внешней области, этот объект может не быть собран, пока жива функция — даже если вы используете только одно поле. Стоит не захватывать лишнее (локальные копии нужных полей), снимать слушатели, обнулять ссылки, когда подписка не нужна. Для обычных коротких функций это редко проблема; критично для долгоживущих обработчиков и больших структур данных.`,
  },
];
