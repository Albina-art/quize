/** Вопросы тренажёра: тема «React: виртуальный DOM». */

export const reactVirtualDomTopic = "React: виртуальный DOM";

export const reactVirtualDomQuestions = [
  {
    topic: reactVirtualDomTopic,
    question: "Что такое виртуальный DOM и чем он отличается от реального DOM?",
    hint: "Подумайте о представлении UI в виде JavaScript-объекта без привязки к браузерному API.",
    answer: `Виртуальный DOM — это программная абстракция, лёгкое JavaScript-представление дерева UI. В отличие от реального DOM, у него нет «тяжёлых» браузерных методов (\`appendChild\`, \`setAttribute\` и т.д.) и каждое изменение не запускает сразу reflow/repaint страницы. Его можно быстро пересоздавать и сравнивать. Реальный DOM — API браузера к HTML-дереву; операции с ним обычно дороже.

Виртуальный слой нужен, чтобы вычислять **минимальный** набор правок реального DOM, а не править документ на каждое мелкое изменение вручную.`,
  },
  {
    topic: reactVirtualDomTopic,
    question: "Как React использует виртуальный DOM для повышения производительности?",
    hint: "Вспомните про diffing, пакетные обновления и минимальные изменения реального DOM.",
    answer: `Кратко шаги:

1. При изменении данных строится **новое** виртуальное дерево.
2. Оно **сравнивается** со старым (diffing / reconciliation).
3. По разнице составляется **минимальный** список операций над реальным DOM.
4. Операции **пакетно** применяются к документу.

Так сокращается число дорогих обращений к реальному DOM и группируются обновления — это критично для отзывчивых интерфейсов.`,
  },
  {
    topic: reactVirtualDomTopic,
    question: "Какую роль играют ключи (key) при сравнении виртуальных деревьев?",
    hint: "Представьте список: без ключей React может пересоздавать узлы при сдвиге порядка.",
    answer: `Ключи помогают React **сопоставить** элементы списка между старым и новым деревом по **стабильному id**, а не только по позиции в массиве.

Без ключей при вставке элемента в начало списка может показаться, что «сдвинулись» все элементы — React пересоздаст узлы. Со стабильными \`key\` (например id из данных) он переиспользует DOM-узлы, меняет порядок или точечно обновляет пропсы.`,
  },
  {
    topic: reactVirtualDomTopic,
    question:
      "Что такое алгоритм сравнения (diffing) в React? Назовите два основных допущения (эвристики), которые ускоряют сравнение.",
    hint: "React не использует полный оптимальный алгоритм с кубической сложностью — он упрощает задачу.",
    answer: `Diffing — это вычисление разницы между двумя виртуальными деревьями, чтобы применить минимум операций к реальному DOM.

Два ключевых допущения:

1. **Разные типы элементов** (\`div\` vs \`span\`) означают **разное поддерево**: старое уничтожается, новое строится заново — глубокое сравнение детей при смене типа не делается.

2. **Стабильные key в списках** позволяют сопоставлять элементы между прошлым и новым рендером и **переиспользовать** узлы.

Благодаря эвристикам сравнение удаётся привести к линейной сложности **O(n)** в типичных случаях вместо потенциально **O(n³)** у «наивного» полного сопоставления деревьев.`,
  },
  {
    topic: reactVirtualDomTopic,
    question:
      "Что происходит в React при изменении состояния компонента? Цепочка от вызова setState до обновления реального DOM.",
    hint: "Очередь обновлений, повторный рендер, новое виртуальное дерево и reconciliation.",
    answer: `Упрощённая цепочка:

1. **setState** / setter из **useState** ставит обновление в очередь (в современном React обновления могут батчиться).
2. Планируется повторный **рендер** компонента и затронутых потомков.
3. Функция компонента возвращает новое дерево элементов (из JSX).
4. Новое виртуальное дерево **сравнивается** со старым (diffing).
5. Вычисляется список **минимальных** операций над реальным DOM.
6. Операции **применяются** к документу (часто пакетно).
7. Браузер выполняет **layout/paint** для затронутых областей.

Точные детали планировщика зависят от версии React (Concurrent Features и т.д.), но логика «новое дерево → diff → патч DOM» сохраняется.`,
  },
  {
    topic: reactVirtualDomTopic,
    question: "Можно ли использовать виртуальный DOM вне React? Приведите примеры.",
    hint: "Это общая идея; есть библиотеки со своей реализацией.",
    answer: `Да. Виртуальный DOM как идея не привязан к React. Примеры:

- **Preact**, **Inferno** — свои реализации и оптимизации.
- **Vue** — свой виртуальный DOM и другой рендер-пайплайн.
- **Snabbdom** — небольшая библиотека только про виртуальный DOM и patch; её использовали в экосистеме Vue и других проектах.

React использует **внутренний** Fiber и представление элементов; это не «тот же» код, что у перечисленных библиотек, но концепция схожа.`,
  },
  {
    topic: reactVirtualDomTopic,
    question:
      "В чём недостатки виртуального DOM? Когда лучше работать напрямую с реальным DOM или без фреймворка?",
    hint: "Тысячи узлов на кадр, статичные страницы, игры и Canvas.",
    answer: `Недостатки:

- **Стоимость сравнения** — на каждое обновление строится новое дерево и выполняется diff; при огромных деревьях и обновлениях **каждый кадр** это может стать узким местом.
- **Память** — нужны текущее и новое описание (и внутренние структуры React).
- **Первый рендер** SPA может быть тяжелее простой загрузки HTML.

Когда чаще обходятся без классического «React на всё дерево»:

- **Статичные** лендинги без сложного состояния.
- **Canvas, WebGL, игры**, анимации тысяч объектов — часто выгоднее императивный цикл или специализированные движки.
- **Точечные** части UI можно обновлять через **ref** и нативный DOM, не раздувая виртуальное дерево.

В больших приложениях React по-прежнему удобен, но границы важно понимать.`,
  },
  {
    topic: reactVirtualDomTopic,
    question:
      "Почему React не генерирует только инкрементальные правки DOM без виртуального дерева? Разве нельзя обновлять только изменённые узлы?",
    hint: "Платформы, декларативность, история; сравните с компиляторами вроде Svelte.",
    answer: `Технически можно: некоторые фреймворки **компилируют** компоненты в тонкий императивный код (Svelte, Solid и др.) и обходятся без классического виртуального DOM.

У React виртуальное дерево и reconciliation связаны с целями:

1. **Несколько целей рендера** — один и тот же модельный код теоретически подходит для DOM, нативных платформ (React Native) и др.
2. **Декларативная модель** — вы описываете результат, движок сам находит diff.
3. **Отладка и инструменты** — проще сравнивать деревья, строить DevTools.
4. **История и архитектура** — ядро давно выстроено вокруг этой модели.

Альтернативы без VDOM часто быстрее в микробенчмарках, но имеют другие компромиссы (экосистема, модель реактивности, DX).`,
  },
];
