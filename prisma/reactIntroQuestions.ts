/** Вопросы тренажёра для темы «React: основы». */

export const reactIntroTopic = "React: основы";

export const reactIntroQuestions = [
  {
    topic: reactIntroTopic,
    question: "Какая основная цель использования виртуального DOM в React?",
    hint: "Подумайте о производительности и о том, как React минимизирует количество операций с реальным DOM.",
    answer: `Виртуальный DOM позволяет React эффективно обновлять пользовательский интерфейс. React создаёт лёгкую копию реального DOM, сравнивает её с предыдущей версией (diffing) и применяет к реальному DOM только те изменения, которые действительно нужны. Это снижает затратные манипуляции с реальным DOM и ускоряет работу приложения.`,
  },
  {
    topic: reactIntroTopic,
    question: "Чем отличаются props от state в React?",
    hint: "Один из них передаётся извне и не должен изменяться компонентом, другой управляется внутри компонента и может меняться.",
    answer: `• **props** передаются от родителя к потомку и доступны только для чтения (immutable). Дочерний компонент не должен напрямую их менять — это делает родитель.

• **state** — внутренние данные компонента; меняются через setState (классы) или setter из useState (функции). Изменение state приводит к повторному рендеру.`,
  },
  {
    topic: reactIntroTopic,
    question: "Что такое JSX и почему он используется в React?",
    hint: "JSX похож на HTML, но написан в JavaScript-файле. Это синтаксический сахар.",
    answer: `JSX — синтаксическое расширение JavaScript: разметка, похожая на HTML, прямо в коде. Обязательным не является (можно писать React.createElement вручную), но код становится нагляднее. Под капотом JSX превращается в вызовы React.createElement(), которые создают React-элементы.

Пример:
\`\`\`jsx
const element = <h1>Привет!</h1>;
// эквивалентно:
const element = React.createElement("h1", null, "Привет!");
\`\`\``,
  },
  {
    topic: reactIntroTopic,
    question: "Для чего используется хук useEffect? Приведите пример типичной ситуации.",
    hint: "Этот хук заменяет методы жизненного цикла componentDidMount, componentDidUpdate, componentWillUnmount в функциональных компонентах.",
    answer: `useEffect выполняет побочные эффекты в функциональных компонентах: запросы к API при монтировании, подписки на события, таймеры, прямую работу с DOM и т.д.

Пример загрузки данных:
\`\`\`jsx
useEffect(() => {
  fetch("/api/users")
    .then((res) => res.json())
    .then(setUsers);
}, []);
\`\`\`

Пустой массив зависимостей [] означает «выполнить при монтировании» (и при необходимости очистить при размонтировании).`,
  },
  {
    topic: reactIntroTopic,
    question: 'Что такое «поднятие состояния» (lifting state up) в React?',
    hint: "Когда нескольким компонентам нужны одни и те же данные, храните состояние в общем предке.",
    answer: `Поднятие состояния — перенос общего state из потомков в ближайшего общего родителя. Родитель держит данные и передаёт их вниз через props и колбэки для обновления. Так один источник истины и проще синхронизировать UI.

Пример: два поля ввода и отображение суммы — значения хранятся в родителе, а не раздельно в каждом поле.`,
  },
  {
    topic: reactIntroTopic,
    question: "Как в React сделать так, чтобы компонент ничего не отображал?",
    hint: "Верните специальное значение из компонента или используйте условный оператор.",
    answer: `Обычно возвращают **null** из компонента или из ветки условия. Также используют условный рендер (\`&&\`, тернарный оператор). Возвращать **undefined** как результат компонента не рекомендуется — возможны предупреждения React.

\`\`\`jsx
function Warning({ isActive }) {
  if (!isActive) return null;
  return <div>Внимание!</div>;
}
\`\`\``,
  },
  {
    topic: reactIntroTopic,
    question: "Какое назначение ключей (key) при рендере списков в React?",
    hint: "Ключи помогают React идентифицировать элементы при изменениях, добавлении или удалении.",
    answer: `**key** — строковый атрибут у элементов в массиве (часто из map). Он помогает reconciliation сопоставлять элементы между рендерами: что изменилось, что добавилось или удалилось. Ключи должны быть стабильными и уникальными среди соседей; предпочтительно id из данных, а не индекс массива (если список меняется).`,
  },
  {
    topic: reactIntroTopic,
    question: "Как передать данные от дочернего компонента родительскому в React?",
    hint: "Поток данных сверху вниз; для обратной связи используйте колбэки.",
    answer: `Напрямую «вверх» данные не передаются: родитель передаёт **функцию-колбэк** в props, а ребёнок вызывает её с нужными аргументами.

\`\`\`jsx
function Parent() {
  const [data, setData] = useState("");
  const handleData = (childData) => setData(childData);
  return <Child onSendData={handleData} />;
}

function Child({ onSendData }) {
  return (
    <button type="button" onClick={() => onSendData("Привет от ребёнка!")}>
      Отправить
    </button>
  );
}
\`\`\``,
  },
  {
    topic: reactIntroTopic,
    question: 'Что такое React.PureComponent и когда его использовать?',
    hint: "Компонент, который отрезает лишние перерисовки при поверхностном сравнении props и state.",
    answer: `**React.PureComponent** (класс) реализует shallow-сравнение новых и старых props и state в shouldComponentUpdate. Если ссылки и примитивы не изменились, рендер пропускается — это оптимизация.

Подходит, когда структуры неглубокие; для глубоких структур поверхностное сравнение может не заметить изменения. У функциональных компонентов близкий аналог — **React.memo**.`,
  },
  {
    topic: reactIntroTopic,
    question:
      "В чём разница между управляемыми (controlled) и неуправляемыми (uncontrolled) компонентами?",
    hint: "Кто хранит значение поля ввода: React или сам DOM?",
    answer: `• **Управляемый (controlled):** значение хранится в state React; в input передаётся value и onChange — единственный источник истины в коде.

• **Неуправляемый (uncontrolled):** значение живёт в DOM; доступ через **ref** и defaultValue; React не синхронизирует каждое нажатие в state.

Пример управляемого:
\`\`\`jsx
const [text, setText] = useState("");
<input value={text} onChange={(e) => setText(e.target.value)} />
\`\`\`

Пример неуправляемого:
\`\`\`jsx
const inputRef = useRef(null);
<input ref={inputRef} defaultValue="Привет" />
// значение: inputRef.current.value
\`\`\``,
  },
];
