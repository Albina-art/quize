# Скопируйте в `.env` или `.env.local` и заполните значения.

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/quiz?schema=public"

# Случайная строка ≥16 символов для подписи cookie-сессии (обязательно для входа и регистрации).
SESSION_SECRET="замените-на-длинную-случайную-строку"
