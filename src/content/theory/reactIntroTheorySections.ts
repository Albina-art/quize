import type { TheorySection } from "@/content/theory/theorySectionTypes";

export const reactIntroTheoryIntro = `
# Что такое React?

**React** — открытая JavaScript-библиотека для построения пользовательских интерфейсов, созданная в **Meta** (ранее Facebook). Первая публичная версия — **2013** год. React помогает собирать сложные интерактивные SPA из переиспользуемых частей и описывать UI **декларативно**: для каждого состояния задаём, что должно быть на экране.

### Кратко об основных идеях

1. **Компоненты** — кирпичики UI: функции или классы, принимают **props** и возвращают дерево элементов.
2. **JSX** — синтаксис, похожий на HTML внутри JS; компилируется в вызовы \`React.createElement\`.
3. **Props** — входные данные **сверху вниз**, дочерний компонент их **не мутирует**.
4. **State** — изменяемое внутреннее состояние; обновление через \`setState\` / \`useState\` вызывает повторный рендер.
5. **Virtual DOM** — лёгкая модель дерева в памяти; React сравнивает версии (**reconciliation**) и минимизирует правки реального DOM.
6. **Хуки** — \`useState\`, \`useEffect\`, \`useContext\`, \`useReducer\`, \`useMemo\`, \`useCallback\` и др. в функциональных компонентах.
7. **Жизненный цикл (классы)** — \`componentDidMount\`, \`componentDidUpdate\`, \`componentWillUnmount\`; в функциях типичная замена — \`useEffect\`.
8. **Однонаправленный поток данных** — предсказуемость и проще отладка.
9. **React Router** — клиентская маршрутизация без полной перезагрузки страницы.
10. **Глобальное состояние** — Redux, Zustand, Context API и др., когда локального \`state\` недостаточно.
`.trim();

export const reactIntroTheorySections: TheorySection[] = [
  {
    title: "Компоненты, JSX, props и state",
    body: `
Компонент инкапсулирует разметку и поведение. **Функциональный компонент** — функция, возвращающая JSX (или \`null\`).

**\`props\`** передаются родителем и для ребёнка **read-only**: менять их «изнутри» нельзя — нужно попросить родителя через колбэк.

**\`state\`** живёт внутри компонента (или выше, если «подняли»). Изменение state планирует ре-рендер; React вызывает компонент снова и обновляет DOM при необходимости.

\`\`\`jsx
function Counter() {
  const [count, setCount] = useState(0);
  return (
    <div>
      <p>Вы нажали {count} раз(а)</p>
      <button type="button" onClick={() => setCount(count + 1)}>
        Нажми меня
      </button>
    </div>
  );
}
\`\`\`
`.trim(),
  },
  {
    title: "Virtual DOM и производительность",
    body: `
При изменении state React строит новое дерево элементов, **сравнивает** его с предыдущим (**diffing**) и вычисляет минимальный набор операций с **реальным DOM**. Это не делает React «быстрее DOM сам по себе», но часто снижает лишние reflow/paint за счёт пакетных обновлений и точечных правок.

Ключи **\`key\`** в списках помогают сопоставлять элементы между рендерами и избегать лишних пересозданий узлов.
`.trim(),
  },
  {
    title: "Хуки и побочные эффекты",
    body: `
- **\`useState\`** — локальное состояние.
- **\`useEffect\`** — побочные эффекты (запросы, подписки, таймеры, работа с DOM); зависимости во втором аргументе задают, когда эффект повторять; функция очистки — при размонтировании или перед следующим запуском.
- **\`useContext\`** — чтение значения контекста без «прокидывания» props через каждый уровень.

\`\`\`jsx
useEffect(() => {
  fetch("/api/users")
    .then((res) => res.json())
    .then(setUsers);
}, []);
\`\`\`

Пустой массив \`[]\` означает «выполнить при монтировании» (и типичный паттерн загрузки данных).
`.trim(),
  },
  {
    title: "Поднятие состояния и однонаправленный поток",
    body: `
Если двум детям нужны общие данные, **state переносят в общего родителя** (**lifting state up**). Родитель передаёт значения через props и функции-обновители вниз.

Обратная связь «ребёнок → родитель» делается через **колбэки в props**: родитель передаёт \`onSomething=\{(data) => ...\}\`, ребёнок вызывает при событии.

Для навигации в SPA обычно используют **React Router**. Для разделяемого состояния между далёкими компонентами — **контекст**, **Redux**, **Zustand** и т.д.
`.trim(),
  },
  {
    title: "Преимущества и экосистема",
    body: `
- Декларативный UI и предсказуемые обновления.
- Сильная экосистема: инструменты, типы, паттерны тестирования.
- **Next.js** — SSR/SSG и маршрутизация на базе React.
- **React Native** — мобильные приложения с похожей моделью компонентов.

Типичные темы для углубления: условный рендер (\`null\`, \`&&\`), контролируемые и неконтролируемые поля, \`memo\` / \`PureComponent\`, производительность списков и ключей.
`.trim(),
  },
];
