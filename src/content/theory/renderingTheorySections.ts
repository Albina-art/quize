import type { TheorySection } from "@/content/theory/theorySectionTypes";

export const renderingTheoryIntro = `
# Рендеринг: от кода до пикселей

**Рендеринг** — многоэтапный процесс превращения кода в пиксели на экране. Понимание этапов помогает писать быстрые интерфейсы. У **чистого HTML/CSS** и у **React** цепочки разные: ниже сначала критический путь браузера, затем фазы React (Fiber) и связь с \`DOMContentLoaded\`.
`.trim();

export const renderingTheorySections: TheorySection[] = [
  {
    title: "Критический путь рендеринга (Critical Rendering Path) в HTML",
    body: `
Это последовательность шагов, которые браузер выполняет, чтобы показать страницу. Её оптимизация — основа быстрой первой отрисовки.

1. **DOM (Document Object Model)** — парсинг HTML в дерево узлов.
2. **CSSOM (CSS Object Model)** — парсинг CSS (внешние и встроенные стили) во второе дерево.
3. **Render Tree** — объединение DOM и CSSOM: только **видимые** узлы с вычисленными стилями.
4. **Layout (Reflow)** — расчёт геометрии: позиции и размеры узлов в координатах страницы.
5. **Paint** — закраска пикселей: фон, текст, границы, тени и т.д.
6. **Composite** — сборка слоёв (например, при \`transform\`, \`opacity\`, \`position: fixed\` в отдельных слоях) и вывод на экран.

Дальше при интерактивности возможны повторные layout/paint при изменении стилей и геометрии — их стоит минимизировать.
`.trim(),
  },
  {
    title: "Фазы рендеринга в React (Fiber)",
    body: `
С React 16+ ядро — **Fiber**: работа режется на мелкие единицы, часть фазы рендера можно **прервать** и вернуться позже (приоритеты, concurrent features).

### 1. Триггер

Рендер запускается когда:

- приложение **монтируется** впервые;
- меняются **props** или **state** (или родитель перерисовался и затронул потомков — зависит от мемоизации).

### 2. Render Phase (фаза рендера)

Происходит **в памяти**, реальный DOM ещё не обновлён. React строит дерево работ (\`workInProgress\`), сравнивает с текущим и вычисляет **минимальный** набор изменений.

- Фаза **прерываемая**: может уступить дорогу более срочным обновлениям (ввод пользователя).
- Внутри условно два прохода: **beginWork** (сверху вниз — вызовы компонентов, пометки изменений) и **completeWork** (снизу вверх — финализация узлов, «всплытие» собранных данных).

### 3. Commit Phase (фаза коммита)

Список изменений применяется к **реальному DOM** **синхронно** и **без прерывания**. Пользователь видит результат после этого шага (до следующего paint браузера — см. хуки ниже).

### Старый стек до React 16

Рендер был **одним длинным синхронным** проходом: тяжёлый компонент мог **блокировать** поток на десятки миллисекунд. Fiber разбивает работу и позволяет чередовать её с обработкой ввода.
`.trim(),
  },
  {
    title: "Хуки, жизненный цикл и соответствие фазам",
    body: `
- **Тело функционального компонента** и **\`render()\`** у классов — выполняются в **Render Phase** (не трогайте DOM и «тяжёлые» подписки здесь осознанно).
- **\`componentDidMount\`**, **\`componentDidUpdate\`** — после мутаций DOM, в **Commit Phase**; удобно для работы с DOM и побочных эффектов, завязанных на уже существующий DOM.
- **\`useLayoutEffect\`** — синхронно **после** мутаций DOM в коммите, но **до** того, как браузер **нарисует** кадр пользователю. Подходит для измерений layout и синхронных правок без «мигания».
- **\`useEffect\`** — **после** отрисовки браузером (асинхронно относительно paint кадра): типичное место для запросов к API, подписок, таймеров — не блокируют первый paint обновления.

Порядок важен: не подменяйте смысл \`useLayoutEffect\` и \`useEffect\` только ради привычки — от этого зависит визуальная стабильность.
`.trim(),
  },
  {
    title: "DOMContentLoaded и React",
    body: `
**\`DOMContentLoaded\`** — событие документа: DOM **уже построен** (HTML распарсен в дерево). На него **не ждут** картинки, стили, iframe; **async**-скрипты тоже не блокируют событие. **Синхронные** скрипты без \`defer\`/\`async\` выполняются во время парсинга и **откладывают** \`DOMContentLoaded\` до своего конца. Если скрипт форсирует вычисление стилей (\`getComputedStyle\`), браузер может дождаться CSS.

**Типичный порядок:**

1. Парсинг HTML → DOM.
2. **\`DOMContentLoaded\`** — DOM готов (CSSOM может быть ещё не готов).
3. Дальше — догрузка CSS, CSSOM, Render Tree, Layout, Paint, Composite.
4. Полная загрузка страницы — событие **\`load\`** (ресурсы вроде изображений).

### Монтирование React

Часто корень монтируют, когда \`#root\` уже есть в разметке:

\`\`\`html
<div id="root"></div>
<script>
  window.addEventListener("DOMContentLoaded", () => {
    const root = document.getElementById("root");
    if (root) {
      // ReactDOM.createRoot(root).render(<App />); — из вашего бандла
    }
  });
</script>
\`\`\`

На практике чаще подключают бандл с \`defer\` или ставят основной \`<script>\` в конце документа — тогда явный \`DOMContentLoaded\` не обязателен: к моменту выполнения DOM уже разобран.

### Фазы React относительно события

Если старт приложения — на \`DOMContentLoaded\`, то **Render / Commit** React идут **после** этого события. Повторные обновления по клику уже **не связаны** с \`DOMContentLoaded\` — оно срабатывает один раз за загрузку документа.

| Этап | \`DOMContentLoaded\` уже произошло? |
|------|-------------------------------------|
| Парсинг HTML | нет |
| DOM готов, событие \`DOMContentLoaded\` | **да, здесь** |
| CSSOM, Render Tree, Layout, Paint | после события |
| \`load\` (картинки и т.д.) | позже |

Итог: \`DOMContentLoaded\` — сигнал «дерево DOM можно безопасно искать из JS»; критический путь отрисовки продолжается дальше; React живёт своими фазами поверх уже готового DOM.
`.trim(),
  },
];
