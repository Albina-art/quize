/** Теория: CORS (Cross-Origin Resource Sharing). */
export const corsMarkdown = String.raw`
# CORS — общий доступ к ресурсам между источниками

**Cross-Origin Resource Sharing (CORS)** — это механизм в браузерах: сервер может **явно разрешить** страницам с **другого origin** читать ответ своего API. Без нужных заголовков интегрированный в страницу **JavaScript** не получит к телу ответа, даже если по сети запрос успешный (**200 OK**).

![CORS — Cross Origin Resource Sharing](/theory/cors-cover.png)

## Что такое origin («источник»)

Origin — это схема + хост + **порт**:

\`\`\`
https://example.com:443/path?x=1
^^^^^^^^^^^^^^ — origin (порт 443 для https обычно не пишут)
\`\`\`

Два URL имеют **один origin**, если совпадают протокол, домен и порт. Страница \`https://a.com/page1\` и \`https://a.com/page2\` — один origin.  
\`https://a.com\` и \`https://api.a.com\` — **разные** origin (поддомен считается отдельным хостом).

## Политика одного источника (Same-Origin Policy)

По умолчанию браузер **ограничивает**, как документ или скрипт с одного сайта может обращаться к данным другого сайта. Это снижает риск: чужая страница не должна просто так читать ответ вашего API с куками пользователя.

Исключения бывают (тег \`<img src="…">\`, \`<script src="…">\`), но **XHR / fetch из JS** подчиняются CORS для кросс-доменных запросов.

## Когда CORS «включается»

Вы на фронте (например, \`https://app.ru\`) вызываете:

\`\`\`js
fetch("https://api.io/users", { headers: { "X-Custom": "1" } });
\`\`\`

Браузер видит **cross-origin** запрос по API. Если ответ сервера **не** содержит согласованных заголовков CORS — **ответ будет скрыт от JS**, в консоли появится типичная CORS-ошибка.

> Важно: **на сервер запрос может дойти нормально**; ограничение касается того, может ли **ваш код на странице** прочитать ответ.

![Схема: preflight OPTIONS и основной cross-origin запрос](/theory/cors-preflight.svg)

## «Простой» запрос и preflight (OPTIONS)

**Простой (simple)** запрос — когда браузер **может** отправить один HTTP-запрос без предварительного **OPTIONS**:

- методы: **GET**, **HEAD**, **POST**;
- только «безопасные» заголовки (\`Accept\`, \`Accept-Language\`, \`Content-Language\` и ограниченный набор других); кастомных вроде \`Authorization\` или \`X-*\` в этом списке нет для simple;
- тело POST: только \`Content-Type\`: \`application/x-www-form-urlencoded\`, \`multipart/form-data\` или \`text/plain\`.

Если метод другой (**PUT**, **DELETE** …), добавлен свой заголовок, или \`Content-Type: application/json\` — запрос становится «не простым»: браузер сначала шлёт **preflight**:

1. Запрос **OPTIONS** на тот же URL (или после редиректа по правилам).
2. В запросе есть заголовки вроде \`Access-Control-Request-Method\`, \`Access-Control-Request-Headers\`.
3. Сервер на **OPTIONS** должен ответить разрешениями: какие методы и заголовки можно, и для какого \`Origin\`.

Если preflight не проходит с точки зрения политики — **основной запрос даже не отправят** (или отправят, но ответ не отдадут JS — смотрите конкретный кейс в DevTools).

## Основные заголовки ответа сервера

| Заголовок | Назначение |
| --- | --- |
| \`Access-Control-Allow-Origin\` | Какой origin может читать ответ. Либо конкретный (\`https://app.ru\`), либо отражение \`Origin\` запроса с белого списка на сервере. **\`*\` несовместим с credentials.** |
| \`Access-Control-Allow-Methods\` | Разрешённые методы (для preflight). |
| \`Access-Control-Allow-Headers\` | Разрешённые нестандартные заголовки запроса. |
| \`Access-Control-Allow-Credentials\` | \`true\`, если разрешены куки / авторизация при cross-origin. |
| \`Access-Control-Max-Age\` | Как долго кэшировать результат preflight. |

Заголовки должны быть **и на ответе к OPTIONS, и на ответе к реальному методу**, где это требуется — иначе браузер заблокирует доступ к ответу.

## Учётные данные (cookies, Authorization)

Если \`fetch(url, { credentials: "include" })\` или в XHR включены credentials:

- Сервер должен отправить **\`Access-Control-Allow-Credentials: true\`**.
- \`Access-Control-Allow-Origin\` **не может быть** \`*\` — только конкретный origin.

Если конфликтует с вашим SPA и API на разных доменах — настраивают CORS аккуратно или выносят API за **общий домен / reverse-proxy**.

## Прокси и «один origin» в разработке

Dev-сервер (Vite, webpack dev server) может **проксировать** \`/api\` на бекенд. Браузер обращается к \`http://localhost:5173/api\` — **тот же origin**, значит политика CORS к этому запросу как к **same-origin** не мешает; прокси уже ходит на реальный сервер без участия браузера в междоменной части.

В продакшене часто делают **nginx** (\`/api → бекенд\`) на том же сайте или настраивают CORS на самом API.

## Краткая памятка

1. CORS нужен там, где **разные origins** и **JavaScript читает ответ**.
2. **Simple vs preflight** — от метода, заголовков и \`Content-Type\`.
3. \`*\` + куки **нельзя**; нужен явный origin + \`Allow-Credentials: true\`.
4. Различайте «запрос ушёл и 200 пришёл» и «ответ **доступен коду страницы**».

---

*Связано с карточками тренажёра и тестами по теме **CORS** в этом проекте.*
`;
