import type { TheorySection } from "@/content/theory/theorySectionTypes";

export const jsClosuresTheoryIntro = `
# Замыкания (closures) в JavaScript

**Замыкание** — это комбинация **функции** и **лексического окружения**, в котором эта функция была создана. Внутренняя функция «помнит» переменные из внешней области видимости даже после того, как внешняя функция уже завершила выполнение.

Это не отдельный синтаксис, а следствие того, что функции в JS являются **полноценными значениями** и при создании получают ссылку на своё окружение (в спецификации — на внешний **Environment Record**).

### Зачем это нужно

- **Инкапсуляция** — «приватные» переменные без классов (паттерн модуль).
- **Фабрики и счётчики** — каждый вызов внешней функции создаёт своё окружение.
- **Колбэки и асинхронность** — обработчики сохраняют доступ к данным на момент подписки.
`.trim();

export const jsClosuresTheorySections: TheorySection[] = [
  {
    title: "Классика: var, цикл и setTimeout",
    body: `
Исторический пример — все таймеры видят **одну** переменную \`i\` из цикла:

\`\`\`javascript
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}
// часто: 3, 3, 3
\`\`\`

У \`var\` область видимости — функция (или глобальная), не итерация цикла. К моменту срабатывания таймеров цикл уже закончился.

С **\`let\`** / **\`const\`** на каждой итерации своё блочное окружение — в колбэке своё \`i\`:

\`\`\`javascript
for (let i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}
// 0, 1, 2
\`\`\`

Альтернатива без смены \`var\` — **IIFE** с параметром или отдельная функция, создающая новое замыкание на каждой итерации.
`.trim(),
  },
  {
    title: "IIFE и модульный паттерн",
    body: `
**IIFE** — Immediately Invoked Function Expression — функция, вызываемая сразу после объявления:

\`\`\`javascript
(function () {
  const secret = 42;
  window.api = { getSecret: () => secret };
})();
\`\`\`

Внутри — своё лексическое окружение; \`secret\` не торчит в глобальной области, но методы всё ещё имеют к нему доступ через замыкание — типичная заготовка под «приватные» поля до появления классов и модулей ES.
`.trim(),
  },
  {
    title: "Память и осторожность",
    body: `
Пока жива функция с замыканием, живёт и **охваченное окружение** (или его часть), нужная для разрешения имён. Поэтому длинные цепочки замыканий на больших объектах могут удерживать память дольше, чем ожидаете — иногда помогает обнулять ссылки или разбивать логику.

В современных приложениях замыкания — повседневность (React-хуки, колбэки, подписки); важно понимать модель, чтобы не удивляться поведению в циклах и асинхронном коде.
`.trim(),
  },
];
