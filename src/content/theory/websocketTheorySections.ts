import type { TheorySection } from "@/content/theory/theorySectionTypes";

export const websocketTheoryIntro = `
# WebSocket

WebSocket — это протокол связи поверх TCP, который позволяет установить **постоянное двустороннее (полнодуплексное) соединение** между клиентом (например, браузером) и сервером. В отличие от классического HTTP, где сервер может ответить только на запрос клиента, WebSocket даёт возможность обеим сторонам отправлять данные в любой момент без повторного согласования соединения.
`.trim();

export const websocketTheorySections: TheorySection[] = [
  {
    title: "Как устанавливается соединение (handshake)",
    body: `
1. **Клиент инициирует HTTP-запрос**  
   Клиент посылает на сервер специальный GET-запрос с заголовком \`Upgrade: websocket\` и заголовком \`Connection: Upgrade\`. Кроме того, в запросе есть случайно сгенерированный \`Sec-WebSocket-Key\`.

\`\`\`http
GET /chat HTTP/1.1
Host: example.com
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==
Sec-WebSocket-Version: 13
\`\`\`

2. **Сервер отвечает подтверждением**  
   Если сервер поддерживает WebSocket, он отвечает HTTP-статусом \`101 Switching Protocols\` и добавляет заголовок \`Sec-WebSocket-Accept\`, вычисленный из присланного ключа по специальному алгоритму.

\`\`\`http
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=
\`\`\`

3. **Соединение переключается на WebSocket**  
   После этого TCP-сокет остаётся открытым, и дальнейший обмен данными идёт уже не по HTTP, а по **кадровому протоколу WebSocket** (frame-based). Оба конца могут отправлять сообщения независимо.
`.trim(),
  },
  {
    title: "Обмен данными",
    body: `
- Данные разбиваются на **фреймы** (frames). Каждый фрейм содержит:
  - opcode (текст, бинарные данные, пинг/понг, закрытие)
  - флаг завершения сообщения (многофреймовые сообщения возможны, но редко нужны)
  - маску (клиент → сервер **обязательно** маскирует данные, сервер → клиент – нет)
  - полезную нагрузку (до ~2⁶³ байт, но обычно ограничено реализацией)

- Сообщение может быть послано в любой момент без дополнительных заголовков HTTP, что снижает задержку и накладные расходы.

- Поддерживаются **контрольные фреймы**: Ping / Pong для проверки живости соединения, Close для корректного завершения.
`.trim(),
  },
  {
    title: "Закрытие соединения",
    body: `
Любая сторона может отправить кадр закрытия (Close frame) с кодом причины. Другая сторона обязана ответить своим кадром закрытия, после чего соединение TCP разрывается. Это позволяет избежать вялотекущих полуоткрытых соединений.
`.trim(),
  },
  {
    title: "Ключевые отличия от HTTP",
    body: `
| Особенность | HTTP/1.1 | WebSocket |
| --- | --- | --- |
| Инициатор обмена | Только клиент | И клиент, и сервер |
| Заголовки | Каждый запрос/ответ содержит заголовки | Только при handshake |
| Стоимость открытия | Новое соединение или keep-alive с задержкой | Одно соединение на весь сеанс |
| Бинарные данные | Base64 или multipart (костыли) | Поддерживаются нативно |
| Очередность | Запрос → Ответ | Асинхронные сообщения в любом порядке |
`.trim(),
  },
  {
    title: "Где применяется",
    body: `
- Онлайн-игры (движение игроков, чат, синхронизация)
- Финансовые тикеры (биржевые котировки в реальном времени)
- Совместная работа над документами (Google Docs)
- Чат-приложения, мессенджеры
- Живые дашборды / мониторинг серверов
`.trim(),
  },
  {
    title: "Ограничения и альтернативы",
    body: `
- **Не кешируется** промежуточными прокси (в отличие от HTTP)
- **Требует постоянного соединения** – на мобильных сетях с частыми переподключениями может быть неэффективным
- Для односторонних обновлений (сервер → клиент) часто проще использовать **Server-Sent Events (SSE)** или **HTTP/2 + Server Push**, если не нужна обратная связь
- На высоких нагрузках может требовать особого масштабирования (балансировщики с поддержкой WebSocket, sticky sessions)
`.trim(),
  },
  {
    title: "Пример простого кода на JavaScript (браузер)",
    body: `
\`\`\`javascript
const socket = new WebSocket('wss://echo.websocket.org');

socket.onopen = function(e) {
  console.log('Соединение установлено');
  socket.send('Привет, сервер!'); // отправка текста
};

socket.onmessage = function(event) {
  console.log('Получено:', event.data);
};

socket.onclose = function(event) {
  console.log('Соединение закрыто');
};
\`\`\`

На сервере (Node.js + \`ws\`) тоже всё симметрично.
`.trim(),
  },
  {
    title: "Итог",
    body: `
WebSocket — это протокол для эффективной двусторонней связи поверх одного TCP-соединения, с минимальными накладными расходами. Он работает через стартовый HTTP handshake, затем переключается на собственный фреймовый формат. Это даёт низкую задержку, поддержку бинарных данных и возможность инициации сообщений любой стороной.
`.trim(),
  },
];
