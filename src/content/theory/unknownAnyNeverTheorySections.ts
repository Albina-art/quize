import type { TheorySection } from "@/content/theory/theorySectionTypes";

export const unknownAnyNeverTheoryIntro = `
# unknown, any и never в TypeScript

В системе типов TypeScript три специальных имени часто путают: **\`any\`**, **\`unknown\`** и **\`never\`**. У каждого своя роль: одно ослабляет проверки, второе наоборот заставляет их проходить перед использованием, третье описывает ситуации «значения не бывает» или помогает компилятору доказать полноту веток.
`.trim();

export const unknownAnyNeverTheorySections: TheorySection[] = [
  {
    title: "\`any\` — «разрешить всё»",
    body: `
**\`any\`** означает: «это может быть чем угодно, и с этим значением можно делать всё, что разрешено для любых типов» — компилятор **почти не проверяет** обращения к такому значению.

- Это **escape hatch** из системы типов: удобно при миграции с JavaScript или при работе с библиотеками без типов.
- **Минус:** теряется смысл статической типизации — ошибки переносятся в рантайм.
- Присвоить \`any\` можно почти откуда угодно; из \`any\` «проходит» что угодно в другие типы без предупреждений — именно это делает его опасным.

\`\`\`typescript
let x: any = JSON.parse("{}");
x.foo.bar.baz(); // компилятор не ругается — упадёт в рантайме, если структура другая
\`\`\`

**Когда уместно:** временно, на границе нетипизированного кода, или осознанно, с последующим сужением типов там, где это реально.
`.trim(),
  },
  {
    title: "\`unknown\` — «что-то есть, но мы ещё не знаем что»",
    body: `
**\`unknown\`** — тип верхнего уровня для «значение есть, но тип мы пока не установили». Это **типобезопасная** альтернатива \`any\`.

- С переменной типа \`unknown\` **нельзя** просто так вызывать методы, обращаться к полям или передавать туда, где ждут конкретный тип — пока вы **не сузите** тип (проверкой, \`typeof\`, \`instanceof\`, пользовательским type guard или утверждением после своей валидации).
- Так компилятор заставляет явно доказать форму данных.

\`\`\`typescript
function readBody(raw: unknown) {
  if (typeof raw === "string") {
    return raw.toUpperCase(); // здесь raw — string
  }
  if (raw && typeof raw === "object" && "id" in raw) {
    return String((raw as { id: unknown }).id);
  }
  throw new Error("unexpected shape");
}
\`\`\`

**Итог:** используйте \`unknown\` для входящих данных (JSON, DOM, внешние API), когда структура не гарантирована типами.
`.trim(),
  },
  {
    title: "\`never\` — «сюда выполнение не доходит» или «пустое множество значений»",
    body: `
**\`never\`** — тип, у которого **нет ни одного допустимого значения**. Он появляется в двух главных ситуациях.

1. **Недостижимый код** — после \`throw\`, бесконечного цикла, либо когда функция **ничего не возвращает нормально** (только выбрасывает):

\`\`\`typescript
function fail(msg: string): never {
  throw new Error(msg);
}
\`\`\`

2. **Исчерпывающая проверка (\`exhaustive checking\`)** — когда в \`switch\` по объединённому типу вы обработали все варианты, и в \`default\` аргумент имеет тип \`never\`; если добавится новый вариант unions, компилятор укажет на непройденную ветку.

\`\`\`typescript
type Shape = { kind: "circle"; r: number } | { kind: "square"; a: number };

function area(s: Shape): number {
  switch (s.kind) {
    case "circle":
      return Math.PI * s.r * s.r;
    case "square":
      return s.a * s.a;
    default: {
      const _exhaustive: never = s;
      return _exhaustive;
    }
  }
}
\`\`\`

**Важно:** \`never\` — это не «любое значение», а наоборот — «значений нет». Его нельзя подставить туда, где ждут, например, \`string\`, без специальных конструкций (никогда не нужно «приводить» к \`never\` для обычных данных).
`.trim(),
  },
  {
    title: "Сводная таблица",
    body: `
| Критерий | \`any\` | \`unknown\` | \`never\` |
| --- | --- | --- | --- |
| Смысл | «что угодно», проверки отключены | «есть значение, тип неизвестен» | «нет значений» / недостижимо |
| Использовать без сужения | да, что угодно | нет | нет присвоения обычным значениям |
| Назначение | совместимость, быстрый JS-код | входящие данные, безопасность | ошибки, полнота \`switch\`, пустые пересечения |
| Рекомендация | по возможности избегать | предпочитать вместо \`any\` на границах | для контрактов «не вернётся» и exhaustiveness |
`.trim(),
  },
  {
    title: "Практические правила",
    body: `
- Заменяйте \`any\` на \`unknown\` там, где вы **не знаете** форму данных — и добавляйте проверки.
- Оставляйте \`any\` только там, где тип действительно произвольный **и** вы принимаете риск (или закрываете долг типизацией позже).
- Используйте \`never\` в возвращаемом типе функций, которые **всегда** прерывают выполнение, и в ветке \`default\` для полного разбора discriminated unions.
- Помните: TypeScript **стирает типы** при компиляции в JS — эти имена работают на этапе разработки и не заменяют рантайм-валидацию (схемы Zod, ручные проверки и т.д.), но \`unknown\` помогает не забыть про проверки в коде.
`.trim(),
  },
];
