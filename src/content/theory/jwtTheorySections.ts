import type { TheorySection } from "@/content/theory/theorySectionTypes";

export const jwtTheoryIntro = `
# JWT, Access, Refresh и авторизация

Конспект про **JSON Web Token**, пару access/refresh, отличие от сессий и от **Bearer** — как способа передать токен в HTTP.

## Словарик

- **JWT** — JSON Web Token, формат токена по стандарту (RFC 7519).

- **Кодирование информации** — превращение данных в вид, удобный для передачи и машинной обработки (например, картинка в строку Base64 и обратно). **Это не мера безопасности:** закодированное легко декодировать (в том числе на публичных сайтах вроде base64decode).

- **Шифрование** — преобразование так, что прочитать может только тот, у кого есть нужный **ключ** (расшифровка).

- **Хэширование** — необратимое преобразование: из хэша исходное сообщение восстановить нельзя; часто используют для проверки целостности и хранения паролей (не путать с «простым» кодированием).

## Bearer — это не JWT

Это **разные понятия**, хотя их часто используют вместе.

- **Bearer** — **способ передать** токен в заголовке: \`Authorization: Bearer <какой-то_токен>\`.
- **JWT** — **формат самого токена** (структура и содержимое по стандарту).

**Аналогия:** Bearer — конверт с пометкой «вручить предъявителю»; JWT — письмо по определённым правилам (заголовок, полезная нагрузка, подпись).

В \`Bearer …\` может быть **любая** строка: opaque-токен (UUID), проприетарный формат, не обязательно JWT. JWT, в свою очередь, можно передавать и в cookie, и в теле запроса (в URL — плохая практика).

**Типичная связка:** Bearer + JWT — популярна в API: стандартная передача в заголовке и самодостаточный токен без серверной сессии на каждый запрос.
`.trim();

export const jwtTheorySections: TheorySection[] = [
  {
    title: "Что такое JWT и из чего он состоит",
    body: `
**JWT (JSON Web Token)** — открытый стандарт (RFC 7519) для компактной передачи утверждений (**claims**) между сторонами. Токен **подписан** (или в расширениях — зашифрован как JWE); payload при этом **кодируется** в Base64URL и **читается** без секрета — туда не кладут пароли и секреты бизнес-логики.

Три части, разделённые точками:

\`\`\`
xxxxx.yyyyy.zzzzz
\`\`\`

1. **Header** — тип \`JWT\` и алгоритм подписи (\`HS256\`, \`RS256\` и т.д.).
2. **Payload** — claims: \`sub\`, роли, \`exp\`, \`iss\` (издатель), \`aud\` и др.
3. **Signature** — подпись заголовка и payload секретом (symmetric) или приватным ключом (asymmetric); проверка — на сервере тем же секретом или публичным ключом.

При **RS256** и других асимметричных алгоритмах сервер подписывает токен **приватным** ключом, а любой сервис проверяет подпись **публичным** — по той же логике «сейфа», что и при HTTPS:

![Метафора асимметричного шифрования: открытый сейф — публичный ключ, закрытый с кодом — приватный; сообщение читает только владелец кода](/theory/asymmetric-encryption-safes.png)

Пример раскодированных частей:

\`\`\`json
// Header
{ "alg": "HS256", "typ": "JWT" }

// Payload (фрагмент)
{ "sub": "1234567890", "name": "Иван Иванов", "admin": true, "exp": 1735689600 }
\`\`\`
`.trim(),
  },
  {
    title: "Access и Refresh токены",
    body: `
### Схема JWT: логин и защищённые запросы

После входа бэкенд, как и при сессиях, **хэширует пароль** и **сверяет с БД**. Затем выдаёт клиенту пару **access** и **refresh** (refresh обычно ведут в БД или в отдельном хранилище для отзыва и ротации). Дальнейшие запросы к API несут **access**-токен (часто JWT в \`Authorization: Bearer …\`): в такой схеме сервер может **сам проверить подпись и срок** (\`exp\` и др.) **без запроса в БД на каждый вызов** — отсюда «stateless» для access.

![Схема JWT: логин, access и refresh, проверка access JWT на сервере без обращения к БД на каждый запрос](/theory/jwt-auth-diagram.png)

### Access token

- **Короткое время жизни** (часто от минут до часа).
- Содержит идентификацию и права; идёт **на каждый** запрос к API (часто в \`Authorization: Bearer …\`).
- После истечения нужно обновить пару или пройти вход заново.

### Refresh token

- **Дольше живёт** (дни, недели); **не** шлётся на каждый запрос к бизнес-API.
- Хранят в **HttpOnly Secure** cookie или в защищённом хранилище приложения; используют только эндпоинт вроде \`/refresh\`.
- При компрометации можно **отозвать** (чёрный список, удаление из БД, ротация).

### Схема работы

1. Логин → сервер выдаёт access + refresh.
2. Запросы к API → с access.
3. Access истёк → клиент шлёт refresh на \`/refresh\`.
4. Сервер проверяет refresh → выдаёт **новую пару** (или только новый access — по политике).
5. Refresh истёк или отозван → полный логин снова.

**Зачем пара:** украденный access даёт окно доступа только до \`exp\`; refresh можно отозвать без смены пароля пользователя.
`.trim(),
  },
  {
    title: "JWT и сессии: что выбрать",
    body: `
### Как устроена сессионная аутентификация (Session Auth)

Логин и пароль в теле запроса → хэш и сверка с БД → выдача \`session_id\` клиенту. Следующие запросы передают идентификатор (на схеме — в **заголовках**; в классических браузерных приложениях чаще тот же принцип через **cookie** с session id). Каждый защищённый запрос бэкенд снова сверяет с хранилищем — модель **stateful**.

![Схема Session Auth: логин, хэш пароля, session_id, проверка в БД при каждом запросе](/theory/session-auth-diagram.png)

| Критерий | JWT (stateless) | Сессии (stateful) |
|----------|-----------------|-------------------|
| Состояние на сервере | Данные в токене; сервер не хранит сессию на каждый запрос | Хранилище сессий (память, Redis, БД) |
| Масштабирование | Удобно горизонтально без общего store сессий | Нужен общий session store для кластера |
| Отзыв до истечения | Сложнее (чёрный список, короткий TTL, версия токена) | Удалить сессию — сразу |
| Объём в запросе | Payload на каждый запрос | Обычно маленький session id |
| Конфиденциальность | Payload виден после Base64 decode; только подпись | Секреты на сервере |

**Ориентиры:** JWT (часто access+refresh) — SPA, мобильные клиенты, микросервисы; классические **серверные** веб-приложения с cookie-сессией — когда важен мгновенный отзыв и простая модель на одном бэкенде.
`.trim(),
  },
  {
    title: "Bearer и другие схемы в HTTP",
    body: `
### Bearer и JWT: главное различие

- **Bearer** — это **схема передачи** токена в HTTP-заголовке:
  \`Authorization: Bearer <токен>\`
- **JWT** — это **формат самого токена** (header.payload.signature).

То есть:

- «Используем **Bearer**» = как передаём токен.
- «Используем **JWT**» = что именно передаём.

В Bearer можно передавать не только JWT, но и opaque-токены (случайные строки/UUID).

### Основные схемы HTTP-авторизации

1. **Basic**  
   \`Authorization: Basic base64(login:password)\`  
   Простая, но устаревающая схема; без HTTPS небезопасна.

2. **Bearer**  
   \`Authorization: Bearer <token>\`  
   Самая частая схема для API; токеном может быть JWT или opaque.

3. **API key**  
   Обычно \`X-API-Key: <key>\` (или query/header по правилам API).  
   Подходит для сервис-сервис и публичных API с квотами.

4. **Digest**  
   Хэш-челлендж вместо отправки пароля в явном виде.  
   Исторически безопаснее Basic, но в современных API используется редко.

5. **OAuth 2.0**  
   Это не схема заголовка, а протокол получения токена.  
   Результат обычно передают через **Bearer**.

6. **mTLS**  
   Аутентификация по клиентскому сертификату на уровне TLS (без \`Authorization\` заголовка).  
   Нишево, но очень сильно для B2B/финтеха и внутренних контуров.

### Мини-сравнение

| Подход | Что передаётся | Безопасность (при корректной настройке) | Где чаще используют |
|--------|----------------|------------------------------------------|---------------------|
| Basic | login/password (в Base64) | Низкая/средняя, только с HTTPS | легаси, простые интеграции |
| Bearer | токен (JWT/opaque) | Средняя/высокая | REST API, SPA, mobile |
| API key | ключ клиента | Средняя | публичные и B2B API |
| OAuth 2.0 + Bearer | access token + scopes | Высокая | «войти через ...», внешние API |
| mTLS | сертификат клиента | Очень высокая | высокозащищённые сервисы |

**Почему в этой таблице нет сессий:** здесь сравниваются именно HTTP-схемы авторизации (в первую очередь через заголовок \`Authorization\`). Классическая сессия обычно передаётся не так: клиент отправляет \`session_id\` в cookie, а сервер хранит и проверяет состояние сессии в своём хранилище.

Практический вывод: для большинства новых API — **Bearer + короткоживущий access + refresh + HTTPS**.
`.trim(),
  },
  {
    title: "Сводка, безопасность и псевдокод",
    body: `
### Сравнение access+refresh и сессий

| | Сессии | JWT access + refresh |
|--|--------|----------------------|
| Stateful | да | по сути stateless для access; refresh часто проверяется по БД |
| Отзыв | да | у refresh — да; у access до \`exp\` — через списки отзыва или короткий TTL |
| Мобильные клиенты | cookie не всегда удобны | Bearer в заголовке привычен |

### Рекомендации

- Refresh — **HttpOnly, Secure, SameSite** (по возможности Strict); не класть в \`localStorage\`.
- Access — по возможности **в памяти** приложения, не в \`localStorage\` (риск XSS).
- **Только HTTPS**, короткий TTL access (например 5–15 минут).
- Механизм **отзыва** refresh (ротация, версия, blacklist).
- В payload JWT не класть секреты; для шифрования содержимого — **JWE**, не «надеяться на подпись».

### Псевдокод выдачи и обновления

\`\`\`python
# логин
access_token = jwt.encode({
    "user_id": user.id,
    "role": user.role,
    "exp": datetime.utcnow() + timedelta(minutes=15),
}, SECRET_KEY)
refresh_token = random_string(64)  # или JWT с длинным exp и id в БД
save_refresh_token_to_db(user.id, refresh_token)
return {"access": access_token, "refresh": refresh_token}
\`\`\`

\`\`\`python
def refresh(request):
    refresh_token = request.get_refresh_token()
    if is_valid_refresh_token(refresh_token):
        user_id = get_user_id_by_refresh(refresh_token)
        new_access = jwt.encode(...)
        return {"access": new_access}
    raise HTTP_401
\`\`\`

**Итог:** JWT с refresh — гибкий вариант для API и распределённых систем; классические сессии остаются сильным выбором там, где критичен мгновенный отзыв и простая серверная модель.

Дополнительно по TLS и сертификатам: [SSL.com](https://www.ssl.com/), материалы по сетям: [networkguru.ru](https://www.networkguru.ru/).
`.trim(),
  },
];
