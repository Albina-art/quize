/** Конспект «URL и браузер»: вступление и секции для сворачиваемых блоков на странице теории. */
import type { TheorySection } from "@/content/theory/theorySectionTypes";

export type { TheorySection };

export const urlBrowserTheoryIntro = `
# Что происходит, когда вы вбиваете URL в браузере?

Кратко: сначала находится IP сервера (DNS), затем устанавливается **TCP**-соединение, поверх него — при HTTPS **TLS**, далее **HTTP-запрос** и **ответ**, после чего браузер **парсит и рисует** страницу.
`;

export const urlBrowserTheorySections: TheorySection[] = [
  {
    title: "1. Браузер ищет сервер для обмена данными (DNS)",
    body: `
Браузеру нужно сопоставить доменное имя с IP. Поиск идёт по шагам:

1. **История / кэш браузера** — недавно открытые сайты.
2. **Операционная система** — файл \`hosts\`, системный DNS-кэш.
3. **Кэш маршрутизатора** (если есть запись).
4. Если нигде нет — запрос к **DNS** (Domain Name System): рекурсивный опрос до получения IP (например, через DNS провайдера или публичный 8.8.8.8).
`.trim(),
  },
  {
    title: "2. Установка TCP-соединения",
    body: `
После известного IP клиент устанавливает надёжное соединение по **TCP** — «тройное рукопожатие»:

1. Клиент отправляет **SYN** — запрос на установление соединения.
2. Сервер отвечает **SYN/ACK** — подтверждает и предлагает соединение.
3. Клиент отправляет **ACK** — соединение считается установленным; дальше можно передавать байты приложения (HTTP/TLS).

Это не шифрование; только надёжность и порядок доставки.
`.trim(),
  },
  {
    title: "3. HTTP-запрос за контентом",
    body: `
Через установленное соединение браузер отправляет **HTTP-запрос**: метод, путь, заголовки (User-Agent, Accept, Cookie и др.), иногда тело.

Почти всегда на сайтах используется **HTTPS**: обмен идёт в зашифрованном виде поверх **TLS** после рукопожатия TLS (сертификат, согласование ключей).

### Разбор URL на примере

Пример:

\`\`\`text
https://www.youtube.com/pavlenkoat?name=1&age=2
\`\`\`

| Часть | Значение |
| --- | --- |
| \`https://\` | Протокол **HTTPS**, по умолчанию порт **443** |
| \`www.youtube.com\` | **Хост** (домен сервера) |
| \`/pavlenkoat\` | **Путь** к ресурсу |
| \`?name=1&age=2\` | **Строка запроса** (query): параметры после \`?\`, разделитель \`&\` |

Для сравнения: \`http://\` — без шифрования по умолчанию, порт **80**.

### HTTPS (HyperText Transfer Protocol Secure)

Это защищённая версия HTTP: **весь обмен между браузером и сервером шифруется**, чтобы не было перехвата или подмены (логины, пароли, страницы).

#### Чем отличается от HTTP?

| Характеристика | HTTP | HTTPS |
| --- | --- | --- |
| Шифрование | Нет (данные открытым текстом) | Да (TLS/SSL) |
| Порт по умолчанию | 80 | 443 |
| Риски | Прослушивание, MITM | Конфиденциальность и целостность выше |
| Типичное использование | Устаревающий открытый HTTP | Современный стандарт для любых сайтов |

#### Как работает HTTPS (упрощённо)

1. Обращение по \`https://…\`
2. Сервер присылает **цифровой сертификат** (CA: Let's Encrypt, DigiCert и т.д.).
3. Браузер проверяет: срок, домен, цепочку доверия.
4. **TLS handshake** — согласование ключей (по смыслу отдельный «диалог» поверх TCP, с криптографией).
5. Дальше заголовки, куки, HTML, картинки — в **зашифрованном виде**.

**Зачем:** конфиденциальность, целостность (подмена заметнее), аутентификация сервера.

Без HTTPS браузеры помечают сайт как небезопасный; многие API (геолокация, Service Workers и др.) требуют безопасный контекст (**HTTPS**).
`.trim(),
  },
  {
    title: "4. Сервер обрабатывает запрос",
    body: `
1. Запрос принимает **веб-сервер** (nginx, Apache и т.п.).
2. **Статика** может отдаваться сразу с диска.
3. **Динамика** передаётся обработчику приложения (PHP-FPM, Gunicorn, Node, Java и т.д.) через FastCGI, прокси и т.п.
4. Приложение обращается к БД, шаблонам и формирует ответ.
5. Веб-сервер возвращает ответ клиенту.
`.trim(),
  },
  {
    title: "5. Ответ браузеру и отрисовка",
    body: `
Браузер получает HTML (и загружает CSS, JS, изображения по ссылкам). Дальше: парсинг **DOM**, **CSSOM**, **Render Tree**, **layout**, **paint**, **composite** — пока страница не отображается на экране. JavaScript может менять DOM и вызывать перерасчёты.

---

*Материал можно использовать вместе с карточками тренажёра по темам DNS, TCP/TLS и браузерного конвейера.*
`.trim(),
  },
];
