/** Теория: HTTPS, SSL/TLS, шифрование и рукопожатие. */
export const httpsTlsMarkdown = String.raw`
# HTTPS, SSL и TLS

Ниже — структурированный конспект: как связаны **HTTPS**, **SSL** и **TLS**, какие задачи решает шифрование, чем отличаются симметричное и асимметричное шифрование и как проходит **TLS Handshake**.

## Что такое HTTPS, SSL и TLS?

**SSL (Secure Sockets Layer)** и его современная версия **TLS (Transport Layer Security)** — это криптографические протоколы, которые обеспечивают безопасное соединение между браузером и сервером. По сути, это «набор правил», который позволяет шифровать данные, чтобы их никто не мог перехватить и прочитать. Сегодня используется именно **TLS**, но название SSL по-прежнему часто применяется как общий термин.

## Основные цели шифрования в HTTPS

Протокол HTTPS с использованием TLS решает три ключевые задачи:

1. **Конфиденциальность:** информация шифруется, поэтому даже если данные будут перехвачены, злоумышленник не сможет их прочитать.
2. **Аутентификация:** есть механизм, с помощью которого можно проверить «личность» участников соединения (чаще всего — подлинность сервера).
3. **Целостность:** данные защищены от подмены в процессе передачи.

## Симметричное и асимметричное шифрование

В основе работы HTTPS лежат два разных типа шифрования.

### Симметричное шифрование

Представьте себе сейф, который можно открыть и закрыть одним и тем же ключом. **Один и тот же секретный ключ** используется и для шифрования, и для расшифровки данных.

- **Главный плюс:** этот метод очень быстрый и отлично подходит для шифрования большого объёма трафика.
- **Главный минус:** возникает проблема безопасной передачи самого ключа.

### Асимметричное шифрование

Это как почтовый ящик. У него есть два ключа. **Публичный ключ (public key)** доступен всем и нужен, чтобы «опустить письмо» (зашифровать данные). А открыть ящик и прочитать письмо может только владелец с **приватным ключом (private key)**.

- **Главный плюс:** решает проблему безопасной передачи ключа, так как публичный ключ можно передавать открыто.
- **Главный минус:** этот метод работает намного медленнее.

## Как работает TLS Handshake (рукопожатие)

HTTPS комбинирует оба типа шифрования, чтобы использовать их сильные стороны. Процесс установки безопасного соединения называется **TLS Handshake**. Ключевые этапы:

1. **ClientHello:** браузер отправляет серверу приветствие: поддерживаемые версии TLS и методы шифрования, а также случайную последовательность байтов — **Client Random**.
2. **ServerHello:** сервер выбирает подходящий метод шифрования из предложенных, отправляет **Server Random** и **SSL/TLS-сертификат**.
3. **Проверка сертификата:** браузер проверяет сертификат через центр сертификации, чтобы убедиться, что сайт подлинный, а не поддельный.
4. **Обмен ключами:** браузер генерирует **Pre-Master Secret**, шифрует его **публичным ключом** из сертификата сайта и отправляет серверу. Расшифровать может только сервер своим **приватным ключом**.
5. **Финал:** браузер и сервер из общего Pre-Master Secret и значений Client Random и Server Random выводят **общий сессионный ключ** для симметричного шифрования.

## Результат работы

После завершения TLS Handshake браузер и сервер переходят на быстрое **симметричное шифрование** с уникальным сессионным ключом. С этого момента все данные (HTML, картинки, куки) передаются в зашифрованном виде до закрытия соединения.
`;
