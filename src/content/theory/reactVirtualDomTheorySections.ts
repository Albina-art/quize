import type { TheorySection } from "@/content/theory/theorySectionTypes";

export const reactVirtualDomTheoryIntro = `
# Virtual DOM в React — подробно

**Virtual DOM** (виртуальная объектная модель документа) — ключевая абстракция React: она помогает сохранять **декларативный** стиль («опиши UI для состояния») и при этом **сокращать** дорогие операции с реальным DOM.

### Проблема реального DOM

Реальный **DOM** — древовидное представление HTML в браузере. Любое изменение через DOM API (\`appendChild\`, правки атрибутов и т.д.) может запускать **reflow**, **repaint** и другие тяжёлые шаги. При частых обновлениях (анимации, потоковые данные) это даёт заметные тормоза.

### Идея виртуального DOM

React держит **лёгкое описание** дерева UI в виде обычных JavaScript-структур (React-элементов). Это не браузерные узлы: нет «тяжёлых» свойств и методов реального DOM, поэтому построить новое дерево и сравнить его с предыдущим можно быстрее, чем многократно трогать документ.

Упрощённый вид узла (идея, не внутренний формат React):

\`\`\`js
{
  type: "div",
  props: {
    className: "container",
    children: [{ type: "h1", props: { children: "Привет" } }],
  },
}
\`\`\`
`.trim();

export const reactVirtualDomTheorySections: TheorySection[] = [
  {
    title: "Алгоритм: рендер, diffing, reconciliation",
    body: `
1. **Начальный рендер** — по виртуальному дереву строится реальный DOM (или поддерево при вставке в контейнер).

2. **Обновление** — при изменении state (\`setState\`, setter из \`useState\`) React вычисляет **новое** виртуальное дерево из вашего JSX.

3. **Сравнение (diffing)** — новое дерево сопоставляется со старым. Эвристики ускоряют работу:
   - если **тип узла** на месте другого (\`div\` → \`span\`), поддерево **пересоздаётся**;
   - при **одинаковом типе** сравниваются пропсы и дети рекурсивно;
   - в **списках** помогают стабильные **\`key\`** — React сопоставляет элементы по id, а не только по позиции.

4. **Reconciliation** — по результату diff вычисляется **минимальный** набор операций с реальным DOM; изменения **пакуются** (batch), чтобы реже заставлять браузер делать лишнюю работу.
`.trim(),
  },
  {
    title: "Почему это помогает производительности",
    body: `
Сравнение двух виртуальных деревьев тоже стоит CPU, но обычно дешевле хаотичных правок реального DOM «вручную». React **группирует** обновления и **минимизирует** число обращений к «тяжёлому» DOM.

Важно: виртуальный DOM **не делает each операцию быстрее по определению** — выигрыш в типичных приложениях от **меньшего числа** дорогих DOM-операций и предсказуемого порядка обновлений при декларативном коде.
`.trim(),
  },
  {
    title: "Ограничения и когда обходить абстракцию",
    body: `
- Очень **большие** или **очень частые** обновления тысяч узлов (сложные SVG, частые перестройки) — виртуальный слой может стать узким местом; тогда помогают **библиотеки под задачу**, **Canvas/WebGL**, точечная работа через **\`ref\`**.
- **Первая отрисовка** SPA может быть тяжелее «голого» HTML из-за построения дерева и гидрации (в SSR/Next.js свои нюансы).
- Для **простых статичных** страниц иногда достаточно HTML/CSS без фреймворка.

Идея виртуального DOM **не уникальна для React**: похожие подходы есть у Preact, Vue, Inferno, Snabbdom и др.; у каждого свои детали реализации.
`.trim(),
  },
  {
    title: "Зачем React не «просто патчит DOM» без VDOM",
    body: `
Можно компилировать обновления в тонкий императивный код (как делают Svelte, Solid и др.). У React исторически другая модель:

- **Переносимость** — та же абстракция элементов подходит для разных целей (веб, React Native и т.д.).
- **Декларативность** — вы описываете желаемое дерево, а движок сам считает разницу.
- **Инструменты** — удобнее отладка и сценарии вроде time-travel в DevTools.

Альтернативы без классического VDOM выбирают другие компромиссы (размер бандла, правила реактивности, младость экосистемы).
`.trim(),
  },
];
